

The following steps are required for adding and removing arrows:

- ArrowType.java
    - add enum constant
    
- moarrows.arrows
    - add class [NAME]ArrowEffect with implements ArrowEffect and applicable includes
    
- MoArrowsPlayerListener
    - add:
        else if (l.contains("[NAME]"))
            stack = materialHandler.removedItemStacks.get("[NAME]"); 
    
- MaterialHandler.java
    - add:
        tempStack = new ItemStack[10];
        s1 = moArrows.getConfig().getString("[NAME]-materials");
        if (s1 != null) {
            parseMaterial(s1, tempStack);
            removedItemStacks.put("[NAME]", tempStack);
        } else {
            removedItemStacks.put("[NAME]", nullStack);
        }
    
- plugin.yml
    - add permissions 
    
- config.yml
    - add [NAME]-materials: 0:0,
    
- ConfigHandler.java
    - edit default config generator (optional)